Stacked on #36. Lands the remaining grok + gemini pending-report items.

- Latte/Paper `--hairline` is darker than `--card` so card borders show
- Live layout abort raised from 4s to 15s (`LIVE_LAYOUT_TIMEOUT_MS`)
- 3D nameplates: `devops` → `OPS` (not `DEV`)
- Sonar: moving blips are visual-only; pick via a static specimen list
- Ask dock + dossier glass `88%`; dossier repo link is a pill with ExternalLink
- Snapshot source pill + agent-offline chip get the “cached layout” tooltip

Not in this PR: giant cat mesh (art rewrite).

Base: `portfolio-gen/2026-08-31-pending-report-fixes` (#36).
