# Pending mirror: fishTank

**Date:** 2026-08-10  
**Block type:** `fishTank`  
**FE:** `src/content/schema.ts`, `src/render/registry.ts`, `design/mirror-manifest.json`  
**BE (to land):** `plugins/portfolio_plugin/schema/ui_layout_schema.py`, `compose/dag.py`, `compose/fish.py`, `compose/block_builder.py`, bake flag `fish_tank_enabled` (default off)

## Contract summary

- `type: "fishTank"`, `props.renderer: "webgl"`
- `props.fish[]` specimens with `species: DomainId` and bounded numerics `size/depth/speed/glow` in `[0,1]`, `school` 0..15, max 40 fish
- Theme via `tankTheme` id (theme registry) — never raw colours
- DAG band: level 3 Architecture

Delete this file once the BE PR merges and vendored `mirror-manifest.json` matches.
